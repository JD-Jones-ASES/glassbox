# Normalize line endings to LF in the repository (avoids CRLF churn between Windows authoring and
# the Linux CI that runs `astro build`). Binary assets are left untouched.
* text=auto eol=lf
*.png binary
*.jpg binary
*.svg text eol=lf
